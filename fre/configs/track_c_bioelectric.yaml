experiment:
  name: "track_c_bioelectric"
  description: "Bioelectric rescue sweep"

# Enable detailed logging for Paper 2 ℂ functional analysis
enable_detailed_logging: true

parameters:
  energy_gradient: [0.40, 0.50]
  communication_cost: [0.20, 0.30]
  plasticity_rate: [0.10, 0.14]
  stimulus_intensity: [0.05, 0.10, 0.15]

rescue_threshold:
  minimum_K: 1.0
  ioU_target: 0.85

output:
  summary_json: "logs/fre_track_c_summary.json"
  results_dir: "logs/track_c"
